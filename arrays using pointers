#include <iostream>
using namespace std;

int main()
{
   int data[9];
   cout << "Enter elements you want to display: ";

   for(int i = 0; i < 9; ++i)
      cin >> data[i];

   cout << "You entered: ";
   for(int i = 0; i < 9; ++i)
      cout << endl << *(data + i);

   return 0;
}
